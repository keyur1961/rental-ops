# Optional overrides. Defaults work for local SQLite (`file:./dev.db`) and `uploads/`.
DATABASE_URL="file:./dev.db"
# DATA_DIR=/data
# UPLOADS_DIR=/data/uploads
OPS_PASSWORD=rentalops
OPS_SESSION_SECRET=brisbane-dev-secret
# SERVER_ACTIONS_ALLOWED_ORIGINS=hire.example.com
